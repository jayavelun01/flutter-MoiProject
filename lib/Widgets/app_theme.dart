import 'package:flutter/material.dart';

class AppColors {
  static const Color seed = Color(0xFF89241C);

  static final ColorScheme lightScheme = ColorScheme.fromSeed(
    seedColor: seed,
    brightness: Brightness.dark,
  );
}
